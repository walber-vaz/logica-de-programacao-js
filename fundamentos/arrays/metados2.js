const numeros = [1, 2, 3, 4, 5];

// Remove elementos dentro de um array
numeros.splice(1, 2)

console.log(numeros)

// Remover ultimo elemento do array e mostra 
console.log(numeros.pop())
console.log(numeros.pop())
console.log(numeros)