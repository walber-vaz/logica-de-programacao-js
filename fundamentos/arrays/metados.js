const numeros = [1, 2, 3, 4, 5];

// Trocar valor do indice do array na posição 0
numeros[0] = 100;

// Adicionar na ultima posição do array 
numeros.push(6);

// junto dos os indice do array com ,
console.log(numeros.join(','))

console.log(numeros);